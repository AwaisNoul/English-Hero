<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#0AA1DD" /> <!-- Transparent fill -->
            <corners android:radius="7dp" /> <!-- Border radius -->
        </shape>
    </item>

    <!-- Black border -->
    <item
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp">
        <shape android:shape="rectangle">
            <stroke
                android:color="#780A76"
                android:width="2dp" />
            <corners android:radius="5dp" /> <!-- Border radius -->
        </shape>
    </item>
</layer-list>
